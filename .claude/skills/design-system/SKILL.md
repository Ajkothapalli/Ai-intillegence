# Skill: design-system

Use this skill for all UI/visual decisions.

## Procedure
1. Consult `design.md` for tokens, components, and patterns
2. Use shadcn/ui primitives — never build custom primitives
3. Apply Tailwind utility classes with the token values from `design.md`
4. Maintain consistency: same component for same pattern everywhere
